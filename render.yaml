services:
  - type: web
    name: rag-api
    runtime: python
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: python backend/run.py

    envVars:
      - key: HF_API_TOKEN
        sync: false

    autoDeploy: true